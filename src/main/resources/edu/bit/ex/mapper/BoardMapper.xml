<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="edu.bit.ex.mapper.BoardMapper"><!--부모클래스-->
   <!-- 마이바티스 -->
   <!-- 자손이 구현하라(BoardMapper interface) -->
   <select id="getList" resultType="edu.bit.ex.vo.BoardVO"><!--VO파일의 GETTER, SETTER객체화-->
   <![CDATA[
      select * from mvc_board order by bGroup desc, bStep asc
   ]]>
   </select><!--;을 빼준다 CDATA 는 XML 구문이 아니다라는 선언이다.-->
   
   
   <insert id="insert"><!--insert는 resultType이 없다-->
   <![CDATA[
     insert into mvc_board (bId, bName, bTitle, bContent, bHit, bGroup, bStep, bIndent)  values (mvc_board_seq.nextval, #{bName}, #{bTitle}, #{bContent}, 0, mvc_board_seq.currval, 0, 0)    
   ]]>
   </insert>
   
   
</mapper>